version: "3.1"
services:
    myapp:
      build: 
        context: .
      container_name: myapp
      working_dir: /app
      restart: always
      volumes:
        - /etc/localtime:/etc/localtime:ro
